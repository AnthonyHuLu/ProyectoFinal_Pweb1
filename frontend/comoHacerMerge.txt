para hacer commits y que cargue aqui hay un algoitmo para resolver el problema
git config pull.rebase false
git pull origin master


otorgar permisos en local..
ls -la .git
sudo chown -R $(whoami):$(whoami) .git
git pull origin master

